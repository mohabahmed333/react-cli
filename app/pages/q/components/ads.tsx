import React from 'react';

interface adsProps {
  // define props here
}

const ads: React.FC<adsProps> = (props) => {
  return (
    <div>
      {/* ads component */}
    </div>
  );
};

export default ads;
